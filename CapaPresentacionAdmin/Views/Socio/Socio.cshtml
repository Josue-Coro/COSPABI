
@{
    ViewBag.Title = "Socio";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="mb-6 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
    <div>
        <h1 class="text-xl font-semibold text-gray-800">Gestión de Socios</h1>
        <p class="text-sm text-gray-400 mt-0.5">Administra los Socios de la cooperativa</p>
    </div>
    <button onclick="abrirCrear()"
            class="btn-primary inline-flex items-center gap-2 px-4 py-2.5 rounded-xl text-sm font-semibold text-white">
        <svg style="width:16px;height:16px;" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
        </svg>
        Nuevo Socio
    </button>
</div>

<!-- Tabla -->
<div class="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden">
    <div class="px-6 py-4 border-b border-gray-100 flex items-center justify-between">
        <span class="text-sm font-semibold text-gray-700">Lista de Usuarios</span>
        <div class="flex items-center gap-2 bg-gray-100 rounded-lg px-3 py-1.5 w-52">
            <svg style="width:15px;height:15px;" class="text-gray-400 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
            </svg>
            <input type="text" id="buscador" placeholder="Buscar socio..."
                   class="bg-transparent text-sm text-gray-600 outline-none w-full placeholder-gray-400">
        </div>
    </div>

    <div class="overflow-x-auto">
        <table class="w-full text-sm">
            <thead>
                <tr class="bg-gray-50 text-left">
                    <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wide">#</th>
                    <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wide">Usuario</th>
                    <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wide">Nombre completo</th>
                    <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wide">Rol</th>
                    <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wide">Fecha creación</th>
                    <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wide">Estado</th>
                    <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wide text-center">Acciones</th>
                </tr>
            </thead>
            <tbody id="bodyUsuarios" class="divide-y divide-gray-100">
                <tr>
                    <td colspan="7" class="px-6 py-10 text-center text-sm text-gray-400">
                        <svg class="animate-spin mx-auto mb-2" style="width:22px;height:22px;" fill="none" viewBox="0 0 24 24">
                            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4" />
                            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v8H4z" />
                        </svg>
                        Cargando usuarios...
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
    <div class="px-6 py-3 border-t border-gray-100">
        <span class="text-xs text-gray-400" id="contadorUsuarios"></span>
    </div>
</div>

<!-- Modal Crear -->
<div id="modalCrear" class="fixed inset-0 z-50 hidden items-center justify-center p-4">
    <div class="absolute inset-0 bg-black/40 backdrop-blur-sm" onclick="cerrarCrear()"></div>
    <div class="relative bg-white rounded-2xl shadow-2xl w-full max-w-lg overflow-hidden">
        <div class="h-1.5 bg-gradient-to-r from-primary-500 to-primary-700"></div>
        <div class="p-6">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-base font-semibold text-gray-800">Nuevo Usuario</h3>
                <button onclick="cerrarCrear()" class="p-1.5 rounded-lg text-gray-400 hover:bg-gray-100 transition">
                    <svg style="width:18px;height:18px;" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
            <iframe id="iframeCrear" src="" style="width:100%;height:480px;border:none;display:block;"></iframe>
        </div>
    </div>
</div>

<!-- Modal Editar -->
<div id="modalEditar" class="fixed inset-0 z-50 hidden items-center justify-center p-4">
    <div class="absolute inset-0 bg-black/40 backdrop-blur-sm" onclick="cerrarEditar()"></div>
    <div class="relative bg-white rounded-2xl shadow-2xl w-full max-w-lg overflow-hidden">
        <div class="h-1.5 bg-gradient-to-r from-primary-500 to-primary-700"></div>
        <div class="p-6">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-base font-semibold text-gray-800">Editar Usuario</h3>
                <button onclick="cerrarEditar()" class="p-1.5 rounded-lg text-gray-400 hover:bg-gray-100 transition">
                    <svg style="width:18px;height:18px;" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
            <iframe id="iframeEditar" src="" style="width:100%;height:510px;border:none;display:block;"></iframe>
        </div>
    </div>
</div>

<!-- Modal Eliminar -->
<div id="modalEliminar" class="fixed inset-0 z-50 hidden items-center justify-center p-4">
    <div class="absolute inset-0 bg-black/40 backdrop-blur-sm" onclick="cerrarEliminar()"></div>
    <div class="relative bg-white rounded-2xl shadow-2xl w-full max-w-sm overflow-hidden">
        <div class="h-1.5 bg-gradient-to-r from-red-400 to-red-600"></div>
        <div class="p-6">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-base font-semibold text-gray-800">Desactivar Usuario</h3>
                <button onclick="cerrarEliminar()" class="p-1.5 rounded-lg text-gray-400 hover:bg-gray-100 transition">
                    <svg style="width:18px;height:18px;" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
            <iframe id="iframeEliminar" src="" style="width:100%;height:230px;border:none;display:block;"></iframe>
        </div>
    </div>
</div>

<style>
    .btn-primary {
        background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
        transition: opacity 0.2s, transform 0.15s, box-shadow 0.2s;
    }

        .btn-primary:hover {
            opacity: 0.92;
            transform: translateY(-1px);
            box-shadow: 0 8px 20px rgba(34,197,94,0.30);
        }
</style>

@section scripts {
    <script>
    function cargarUsuarios() {
        $.ajax({
            url: '@Url.Action("ListarUsuarios", "Usuario")',
            type: 'GET',
            success: function (res) {
                var filas = '';
                if (!res.data || res.data.length === 0) {
                    filas = '<tr><td colspan="7" class="px-6 py-10 text-center text-sm text-gray-400">No hay usuarios registrados.</td></tr>';
                    $('#contadorUsuarios').text('');
                } else {
                    $.each(res.data, function (i, u) {

                        var badge = u.estado
                            ? '<span class="px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-700">Activo</span>'
                            : '<span class="px-2 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-500">Inactivo</span>';

                        var fecha = u.fecha_creacion
                            ? new Date(parseInt(u.fecha_creacion.replace('/Date(', '').replace(')/', '')))
                                .toLocaleDateString('es-BO', { day:'2-digit', month:'2-digit', year:'numeric' })
                            : '—';

                        // Iniciales para el avatar
                        var iniciales = (u.nombre.charAt(0) + u.apellido.charAt(0)).toUpperCase();
                        var nomEsc    = (u.nombre + ' ' + u.apellido).replace(/'/g, "\\'");

                        filas += '<tr class="hover:bg-gray-50 transition fila-usuario">' +
                            '<td class="px-6 py-3.5 text-gray-400 text-xs">' + (i+1) + '</td>' +
                            '<td class="px-6 py-3.5">' +
                            '  <div class="flex items-center gap-3">' +
                            '    <div class="w-8 h-8 rounded-full bg-primary-100 flex items-center justify-center text-primary-700 text-xs font-bold flex-shrink-0">' + iniciales + '</div>' +
                            '    <span class="font-medium text-gray-800">' + u.usuario + '</span>' +
                            '  </div>' +
                            '</td>' +
                            '<td class="px-6 py-3.5 text-gray-600">' + u.nombre + ' ' + u.apellido + '</td>' +
                            '<td class="px-6 py-3.5">' +
                            '  <span class="px-2 py-0.5 rounded-full text-xs font-medium bg-blue-50 text-blue-600">' + (u.rol ? u.rol.nombre : '—') + '</span>' +
                            '</td>' +
                            '<td class="px-6 py-3.5 text-gray-400 text-xs">' + fecha + '</td>' +
                            '<td class="px-6 py-3.5">' + badge + '</td>' +
                            '<td class="px-6 py-3.5 text-center">' +
                            '  <div class="inline-flex items-center gap-1">' +
                            '    <button onclick="abrirEditar(' + u.id_usuario_admin + ')" title="Editar"' +
                            '            class="p-1.5 rounded-lg text-gray-400 hover:bg-green-50 hover:text-green-600 transition">' +
                            '      <svg style="width:15px;height:15px;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/></svg>' +
                            '    </button>' +
                            '    <button onclick="abrirEliminar(' + u.id_usuario_admin + ',\'' + nomEsc + '\')" title="Desactivar"' +
                            '            class="p-1.5 rounded-lg text-gray-400 hover:bg-red-50 hover:text-red-500 transition">' +
                            '      <svg style="width:15px;height:15px;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728A9 9 0 015.636 5.636m12.728 12.728L5.636 5.636"/></svg>' +
                            '    </button>' +
                            '  </div>' +
                            '</td>' +
                            '</tr>';
                    });
                    $('#contadorUsuarios').text(res.data.length + ' usuario(s)');
                }
                $('#bodyUsuarios').html(filas);
            },
            error: function () {
                $('#bodyUsuarios').html('<tr><td colspan="7" class="px-6 py-10 text-center text-sm text-red-500">Error al cargar los datos.</td></tr>');
            }
        });
    }

    $('#buscador').on('input', function () {
        var t = $(this).val().toLowerCase();
        $('.fila-usuario').each(function () {
            $(this).toggle($(this).text().toLowerCase().indexOf(t) !== -1);
        });
    });

    function abrirCrear()  { $('#iframeCrear').attr('src','@Url.Action("CrearUsuario","Usuario")'); $('#modalCrear').removeClass('hidden').addClass('flex'); }
    function cerrarCrear() { $('#modalCrear').removeClass('flex').addClass('hidden'); $('#iframeCrear').attr('src',''); }

    function abrirEditar(id)  { $('#iframeEditar').attr('src','@Url.Action("EditarUsuario","Usuario")?id='+id); $('#modalEditar').removeClass('hidden').addClass('flex'); }
    function cerrarEditar()   { $('#modalEditar').removeClass('flex').addClass('hidden'); $('#iframeEditar').attr('src',''); }

    function abrirEliminar(id, nombre)  { $('#iframeEliminar').attr('src','@Url.Action("EliminarUsuario","Usuario")?id='+id+'&nombre='+encodeURIComponent(nombre)); $('#modalEliminar').removeClass('hidden').addClass('flex'); }
    function cerrarEliminar()           { $('#modalEliminar').removeClass('flex').addClass('hidden'); $('#iframeEliminar').attr('src',''); }

    function onAccionCompletada() { cerrarCrear(); cerrarEditar(); cerrarEliminar(); cargarUsuarios(); }

    $(function () { cargarUsuarios(); });
    </script>
}
